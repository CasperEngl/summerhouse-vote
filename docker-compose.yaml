services:
  sommerhus-vote:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - voting_data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  voting_data:
